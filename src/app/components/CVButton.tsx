import React from "react";

const CVButton = () => {
  return (
    <button className="rounded-xl bg-gray-900 text-gray-100 font-semibold py-2 px-4 transition duration-100 ease-in-out transform hover:bg-gray-800 active:scale-[0.98]">
      Download CV
    </button>
  );
};

export default CVButton;
